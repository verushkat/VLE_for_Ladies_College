import { ChangeDetectorRef, AfterContentInit, OnDestroy, OnChanges } from '@angular/core';
import { Subject, BehaviorSubject } from 'rxjs';
export declare class NgxFloatButtonComponent implements AfterContentInit, OnDestroy, OnChanges {
    private cd;
    state: BehaviorSubject<any>;
    icon: string;
    direction: string;
    spaceBetweenButtons: number;
    open: Subject<boolean>;
    color: string;
    disabled: boolean;
    events: Subject<any>;
    buttons: any;
    private _destroy$;
    constructor(cd: ChangeDetectorRef);
    toggle(): true;
    private checkDirectionType;
    private animateButtons;
    private getTranslate;
    ngAfterContentInit(): void;
    ngOnChanges(changes: any): void;
    ngOnDestroy(): void;
}
